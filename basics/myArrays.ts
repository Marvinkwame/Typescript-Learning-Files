const players: string[] = []
const threes: Array<number> = []

type Use = {
    name: string,
    isActive: boolean
}

const allUsers: Use[] = []

const MLModels: number[][] = [
    [300, 300, 300],
    []
]

players.push("Neymar")
threes.push(34)
allUsers.push({name: "Kakashi", isActive: true})